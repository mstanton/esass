{
  "hooks": {
    "PostToolUse": [
      {
        "command": "python -m esass.hooks.post_tool_use",
        "timeout": 5000
      }
    ],
    "SessionStart": [
      {
        "command": "python -m esass.hooks.session_start",
        "timeout": 10000
      }
    ]
  }
}
